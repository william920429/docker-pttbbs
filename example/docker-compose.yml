services:
  pttbbs:
    image: ghcr.io/william920429/pttbbs:latest
    container_name: pttbbs
    restart: unless-stopped
    ports:
      - 3000:3000
    volumes:
      - data:/home/bbs
    env_file: .env

volumes:
  data:
